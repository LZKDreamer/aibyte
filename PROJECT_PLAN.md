# AI 内容生产项目总体实施计划

> 本文是本仓库的长期项目蓝图。新的 Agent 或新会话开始工作前，应先阅读本文，并以“当前状态”和“下一步”两节为准。
>
> 最后更新：2026-08-22

## 1. 项目目标

建立一套由 Agent 按需调用的 AI 内容生产系统。用户只需要给出方向（也可不给方向，直接指定“生成 AI 日报”或“生成 AI 长文”），Agent 完成资料采集、选题、写作、素材准备、公众号排版和视频制作；用户负责最终浏览、复制发布，以及把成品提交到内容仓库。

系统交付三类成品：

1. **AI 日报**：当天重要的 AI 模型、Agent、开源、工具、产品、研究及行业动态。
2. **AI 长文**：围绕一个 AI 主题的原创解释、教程、案例或方法论文章。
3. **口播视频**：输入一篇完成的文章，自动形成适合内容的口播稿、配音、字幕、分镜和成片。

最后建设内容展示网站，自动展示已提交至 GitHub 内容仓库的文章和视频。

## 2. 已确认的产品边界

### 2.1 触发方式

- 所有内容生产均由用户**显式调用对应 Skill**触发；不做无人值守的每日自动发文。
- Agent 负责成品生产；用户只做最终发布检查、复制到公众号/视频平台，以及提交内容仓库。
- 用户可在调用时指定方向、读者、风格、篇幅或视频类型；未指定时 Agent 使用默认编辑策略。

### 2.2 项目拆分

日报与长文共用采集、资料整理、写作和排版能力，因此合并为一个工程；视频和网站保持独立，不共享代码、不互相自动调用。

```text
aibyte/
├─ ai-content/            # 最终项目：AI 日报与 AI 长文 Skill
├─ ai-video/              # 最终项目：文章转口播视频 Skill
├─ ai-site/               # 最终项目：展示网站
├─ research-pipeline/     # 当前进行中的采集与素材可行性验证工具
├─ PROJECT_PLAN.md        # 本文
└─ docs/                  # 后续补充跨项目的数据格式说明
```

`research-pipeline` 不是面向用户的最终产品。它用于先验证来源采集、页面结构、素材发现和下载；验证成熟后，将稳定的逻辑移入 `ai-content`，保留研究工具作为回归测试。

### 2.3 发布与存储

- GitHub 内容仓库保存：Markdown、公众号 HTML、文章元数据、来源记录、字幕 JSON、封面和少量压缩小图。
- **不把长期 MP4、原始音频和大素材提交到 GitHub。**普通 Git 有单文件和仓库规模约束，长期视频归档会使仓库不可维护。
- 零付费长期方案：最终视频上传至哔哩哔哩或 YouTube；网站嵌入播放器或提供跳转链接；本地电脑保存 MP4 成片、原始素材和工程文件。
- 网站只从 GitHub 内容仓库读取已发布的元数据和文章；Git 提交后自动刷新网站内容。

### 2.4 免费约束

- 不使用 Vercel Blob、R2 等会在超出免费额度后收费的存储作为长期视频库。
- Vercel Blob 的 Hobby 免费额度只有 1GB 存储与 10GB/月数据传输，超额后服务暂停，不适合本项目的视频积累。
- 如网站只用于个人、非商业用途，可使用 Vercel Hobby。Vercel 的 Hobby 条款限制为个人非商业用途；如果以后接广告、课程、商单或其他盈利活动，必须重新选择符合条款的部署方式。
- 网站必须是纯静态展示站，不能承担视频渲染、爬虫或 AI 生成任务。
- Agent、模型、网络和本地硬盘本身可能有既有订阅、额度或硬件成本；“零付费”指不新增托管存储和服务器费用。

## 3. 内容与素材原则

### 3.1 编辑原则

- AI 日报追求及时、清晰和信息密度；AI 长文追求独立结构、解释和实操价值。
- 不直接拼接或改写第三方全文。长文必须增加独立的框架、判断、案例或操作步骤。
- 文章对外不展示 X 链接；日报的原始链接仅记录在 `sources.json`，不为了替换或核查榜单条目而额外寻找官网、产品页或新闻站。
- `Humanizer-zh` 用于消除机械表达、翻译腔和套话，不用于伪造作者身份或规避检测。
- `gzh-design-skill` 只承担最终公众号 HTML 排版，不承担写作。

### 3.2 轻量来源策略

日报不做高成本、多轮事实考据。每条入选项至少在 `sources.json` 保留：

```text
标题、原始链接、抓取时间、发布账号/网站、内容摘要、内容类型
```

长文或另行指定的研究任务可按以下类型评估来源：

- `official`：产品、实验室、论文作者、GitHub 项目或机构的直接发布。
- `reported`：可靠资讯/社区的转述或分析。
- `opinion`：个人观点、测评、推测或传闻。

正式文章可将 `official` 和 `reported` 写成已知信息；`opinion` 只可标注为“社区讨论”或完全不采用。

### 3.3 素材策略

- 先建立候选内容和素材清单，再按**已选主题**下载需要的图片、视频缩略图、演示截图或录屏；不批量囤积整站无关媒体。
- 一个素材包必须记录来源页面、下载时间、文件类型、用途和文章 ID，避免以后混淆。
- 每个下载素材还必须标记为 `evidence`（直接支持某项事实）、`illustration`（与主题相关的视觉说明）或 `decorative`（仅用于氛围/封面）。`illustration` 和 `decorative` 不得被文章表述为事实依据。
- 知识讲解视频优先使用自己生成的信息卡、流程图、数据图和必要截图；实操教程优先使用真实操作录屏、步骤截图和局部放大。
- 页面公开可访问不等同于可任意再发布。即使暂不把版权审查做成首期阻塞项，也必须保留素材来源，以便出现平台问题时可替换。

## 4. 数据来源设计

### 4.1 X榜单的角色

对 AI 日报而言，X榜单赛道榜是固定的选题和排序来源；它不是日报以外所有内容任务的唯一数据源。

- **赛道榜**：AI 日报固定使用 `24小时 / 综合热度 / AI`，尤其适合模型发布、Agent、开发工具、价格变化、开源与产品更新。
- **长文榜**：作为 AI 长文的候选池。它含有大量非 AI 内容，必须先做 AI 主题筛选和价值评分。
- 采集方式：Playwright 浏览器自动化，读取页面渲染后的文字、链接、媒体 URL 和排序/时间筛选结果。

### 4.2 长文与指定研究任务的补充来源

以下来源可用于 AI 长文或用户明确指定的研究任务；它们不进入 AI 日报的候选池，也不改变赛道榜排序：

| 类别 | 优先来源 | 用途 |
| --- | --- | --- |
| 官方发布 | OpenAI、Anthropic、DeepSeek、Kimi、Google AI、Meta AI、Microsoft AI、xAI 等官网 | 模型、产品、API、价格、能力更新 |
| 开源与开发 | Hugging Face、GitHub、模型/工具 Release、论文发布页 | 模型、数据集、Skill、Agent、开源工具 |
| 产品发现 | Product Hunt、OpenRouter、AI 工具官网 | 新工具、产品功能和应用案例 |
| 编辑精选 | The Rundown AI、TLDR AI | 选题补充和重要性判断，不直接复述原文 |
| 中文社区热点 | X榜单赛道榜、长文榜 | 中文用户关注点、实操案例和潜在长文主题 |

长文或指定研究任务的采集优先级：官方 API/RSS/页面 > GitHub Release/模型卡 > 资讯站页面 > 社区内容。Playwright 是没有结构化接口时的浏览器采集兜底。

### 4.3 日报与长文的选题规则

**日报不是多源评分后的官方新闻摘要。** 它固定使用赛道榜的 `24小时 / 综合热度 / AI`：页面的热度顺序决定当天的 8–12 条内容和第一条封面主题。先查看榜单列出的信源；前 1–3 条可用 Playwright 查看对应 X 原帖以补充细节或素材。遇到登录墙、访问受限或浏览器会话不可用时，Agent 必须提示用户并暂停，等待用户决定继续登录、改用榜单摘要或跳过该条；不得自行扩展官网或其他网站核查。官网和 X 原帖不改变榜单排序，也不因没有官网公告而淘汰。

**长文**固定使用长文榜页面，先筛 AI 相关条目，再按下列维度挑选可扩展的话题：

```text
时效性 × 重要性 × 实用性 × 读者相关度 × 可讲解性 − 重复度 − 传闻风险
```

长文候选池采用分层漏斗，避免把所有原始条目交给模型阅读：

```text
原始候选（通常 100–200 条，规则程序处理）
→ 规则去重、时效过滤和关键词分类（约 20–30 条）
→ 同主题合并与多样性筛选（约 8–12 条）
→ Agent 只读取入选条目的资料包，产出一篇长文
```

日报不强行要求类别均衡，也不强行总结趋势；只要是榜单前列的 AI 热点，便按顺序写成独立新闻卡。

## 5. `ai-content` 实施方案

### 5.1 Skill 入口

- `ai-daily`：用户明确调用后，固定生成赛道榜 `24小时 / 综合热度 / AI` 的中文日报包。
- `ai-longform`：输入可选方向、目标读者和文章类型；输出完整长文包。

两者共用内容交付目录与公众号排版能力；日报与长文保留各自的候选来源和编辑规则。

### 5.2 日报工作流

```text
赛道榜：24小时 / 综合热度 / AI
→ 保留前 8–12 条及原始排序；只有用户明确要求时才做单条测试
→ 优先查看条目列出的信源；前 1–3 条用 Playwright 打开对应 X 原帖补充细节或素材
→ 页面受限时提示用户并等待决定；用户同意后才使用榜单摘要继续，不做外部核查
→ 人工判断候选图片与内容的关联性、清晰度、完整性；没有合格图片就纯文字
→ 第一条的合格图片作为封面；图片只插入对应新闻卡一次，按 `01`、`01-1` 命名并登记
→ 以第一条的具体变化拟定标题；单条测试不虚构未写入的条目数量
→ 写短开头和独立新闻卡，正文直接陈述事实，不暴露“榜单称／主帖称／关联帖”等采编过程
→ 公众号 HTML 排版（不出现 X 链接）
→ 输出 Markdown、HTML、来源与素材包；运行素材登记和交付验收脚本
```

### 5.3 长文工作流

```text
采集长文候选和官方资料
→ 识别可写角度（教程/解释/案例/工具评测）
→ 资料包和文章大纲
→ 原创初稿
→ 加入步骤、例子、图表或演示素材建议
→ 中文编辑、来源检查、公众号 HTML 排版
→ 输出文章包
```

### 5.4 成品交付格式

```text
<article-id>/
├─ article.md             # 网站正文
├─ wechat.html            # 可直接复制进公众号编辑器
├─ sources.json           # 候选和引用来源
├─ materials.json         # 实际采用的素材及其用途
└─ assets/
   ├─ 01.jpg              # 按文章顺序编号；也可以没有图片
   └─ materials.md        # 对应内容、文章位置和视频使用说明
```

### 5.5 成功标准

- 用户一次调用即可获得可阅读、可复制的 Markdown 和公众号 HTML。
- 每个日报条目都能在 `sources.json` 找到赛道榜与可用信源记录；不要求逐条官网验证。
- 无重复热点、无无关资讯、无榜单未提供且无法解释的具体断言。
- HTML 不含 X 链接，图片只在关联且清晰时使用，素材说明完整，并通过日报输出验收脚本。

## 6. `ai-video` 实施方案

### 6.1 输入与输出

该项目是独立项目。它接受任意文章 Markdown、HTML 或纯文本，不要求文章由 `ai-content` 生成。

```text
输入文章
→ 文章类型与时长判断
→ 口播稿、分镜、素材清单
→ 配音
→ 词级时间戳字幕
→ 视频渲染
→ 输出 MP4、封面、SRT、字幕 JSON、素材清单
```

### 6.2 视频表现方向

用户提供的截图仅作为表现形式参考，不复制其 UI、品牌或具体设计。可提炼的形式特征：深色背景、强结论大字、关键词高亮、动态信息卡、流程/对比图和底部字幕。

首期需要制作两套视觉模板，具体颜色、字体、动效密度、角色、音乐和封面样式必须在实现前再次向用户确认：

1. **AI 知识讲解模板**：观点、概念、对比、流程图和信息卡为主。
2. **AI 实操教程模板**：真实录屏、步骤编号、局部放大、鼠标指引和操作字幕为主。

### 6.3 技术路径

- 首期只采用 HyperFrames，避免同时维护 HyperFrames 与 Remotion 两套视频工程。
- 文章先转为口播稿；长度由文章结构、视频类型和需要的演示步骤决定，不固定时长。
- TTS 先使用免费可替换适配器；默认尝试用户指定的 Edge TTS 项目。
- 音频生成后，再用 faster-whisper 的词级时间戳转录实际音频，生成同步字幕；不能根据文字长度估计字幕时间。
- 本地渲染视频，不能在 Vercel 函数中渲染。

### 6.4 成功标准

- 输入文章后能得到结构完整、时长自然的口播稿。
- 字幕与实际音频同步，提供 SRT 和 JSON。
- 生成的 MP4 可直接上传到哔哩哔哩或 YouTube。
- 视频素材和来源能在同一成品包中找到。

## 7. `ai-site` 实施方案

### 7.1 职责

网站只展示已发布内容，不采集、不生成、不渲染视频、不保存后台数据库。

- 读取 GitHub 内容仓库的 `index.json` 与文章目录。
- 展示日报、长文和视频的列表、详情、标签和搜索。
- 视频使用哔哩哔哩/YouTube 的嵌入播放器或外链。
- GitHub 内容仓库更新后，自动重新构建静态网站。

### 7.2 免费部署前提

- 首选纯静态站点，不使用运行时函数。
- 若继续使用 Vercel，必须确认站点是个人、非商业用途。
- 若后期有商业化需求，迁移到满足相应条款的静态托管方案；不要在免费个人计划上规避规则。

### 7.3 成功标准

- 提交一篇文章目录和更新索引后，网站自动展示新内容。
- 文章和视频可在移动端阅读/播放。
- 站点不依赖私有数据库、服务器或付费大文件存储。

## 8. 跨项目数据契约

各项目保持独立，但成品文件必须遵循统一数据格式。后续在 `docs/contracts/` 定义 JSON Schema。

最低字段：

```json
{
  "id": "2026-08-22-example-topic",
  "type": "daily | longform",
  "status": "draft | published",
  "title": "文章标题",
  "summary": "文章摘要",
  "publishedAt": "2026-08-22",
  "tags": ["Agent", "AI工具"],
  "cover": "assets/cover.webp",
  "video": {
    "platform": "bilibili | youtube",
    "id": "可选视频ID",
    "url": "可选视频URL"
  }
}
```

## 9. 当前调研状态

### 已完成：X榜单 Playwright 验证

已在 `research-pipeline/` 安装 Playwright 和 Chromium，新增并运行：

- `capture-xbangdan.mjs`
- `output/lanes.png`、`output/lanes.json`
- `output/articles.png`、`output/articles.json`

本次验证结果：

| 页面 | HTTP 状态 | 观察结果 |
| --- | --- | --- |
| `https://xbangdan.com/lanes/` | 200 | 可读取 AI 排名、摘要、热度、时间、关联账号、X 链接；本页捕获 32 个媒体资源。 |
| `https://xbangdan.com/articles/` | 200 | 可读取卡片式长文标题、作者、曝光和收藏；7 天视图显示 213 篇、11 页；本页捕获 103 个媒体资源。 |

结论：赛道榜的筛选条件、排序与页面媒体发现已跑通；它已成为 AI 日报的固定输入。多源适配器仅保留给长文或指定研究任务，不参与日报选题。

### 已完成：多源探测、候选池与样本素材包（供长文与研究任务复用）

已新增并运行：

- `research-pipeline/probe-sources.mjs`：多来源可访问性探测。
- `research-pipeline/build-candidates.mjs`：来源条目归一化、基础分类和候选池生成。
- `research-pipeline/select-daily.mjs`：读取赛道榜结果，按榜单顺序输出日报短名单。
- `research-pipeline/build-sample-pack.mjs`：针对已选候选下载实际素材并写出研究包。

当前 Playwright 可稳定读取的来源：Anthropic News、DeepSeek News、Kimi Code 更新页、Hugging Face Blog、GitHub Trending、Product Hunt AI、The Rundown AI 和 TLDR AI 最新期。OpenAI 新闻页在本机 Chromium 中出现连接关闭，后续需补充官网页面的备用采集方式，不能让它阻塞整个流程。

当前研究产物：

- `output/source-probe/report.json`：各来源访问结果。
- `output/candidates.json` 与 `output/candidate-report.md`：当前 117 条基础候选记录，覆盖官方、开源、编辑精选与中文社区热点。
- `output/daily-shortlist.json` 与 `output/daily-shortlist.md`：赛道榜 `24小时 / 综合热度 / AI` 的顺序短名单；这一步是本地确定性处理，不会逐条消耗 Agent token。
- `output/sample-packs/openai-sol-price-cut/`：一份已下载缩略图、素材 URL、文件大小、SHA-256 和候选元数据的研究包。

结论：赛道榜采集、关联媒体发现与日报短名单已跑通。样本缩略图已人工确认与 Sol 名称有语义关联，但不能证明价格事实，故只能标记为 `illustration`；没有相关且清晰的素材时，日报允许纯文字。

### 已完成：AI 日报 Skill 第一版

日报 Skill 已创建于 `ai-content/.agents/skills/ai-daily/`，仅在明确调用 `$ai-daily` 时执行。日报选题固定来自赛道榜的“24小时 / 综合热度 / AI”，按榜单顺序写 8–12 条独立动态；用户明确要求时可只输出第一条用于版式测试。榜单列出的信源和对应 X 原帖只补充细节与素材，不改变选题或排序；页面受限时必须提示用户并等待决定，不自行扩展到其他网站核查。

日报 Skill 还内置两层编辑规则：Humanizer-zh 的“事实不动、删空话、打破模板句式”作为成稿后的最后编辑关；AIBYTE「信号绿」作为原创公众号主题。后者只借鉴 gzh-design-skill 的信息层级与微信兼容方法，不复制其主题代码。

- `ai-content/scripts/create-daily-output.mjs`：按日期创建不可互相覆盖的日报交付目录。
- `ai-content/.agents/skills/ai-daily/scripts/register-daily-material.mjs`：登记人工确认相关且清晰的素材，按文章位置编号，并更新素材清单。
- `ai-content/.agents/skills/ai-daily/scripts/validate-daily-output.mjs`：检查必备文件、素材命名与登记、`preview.png`、HTML 中的 X 链接，以及 Markdown/HTML 图片引用是否一致。
- 每次日报生成 `article.md`、`wechat.html`、`sources.json`、`materials.json` 与 `assets/materials.md`；没有合格图片时允许没有图片文件。
- Skill 已通过结构验证；输出目录与交付验收已实际运行验证。

### 已完成：AI 日报首篇实测与规则校准

已生成 `2026-08-22` 的完整日报实测：保留赛道榜前 12 条的原始顺序，生成 `article.md`、`wechat.html`、`sources.json`、`materials.json`、`assets/materials.md` 和对应素材。定稿标题为“OpenAI Sol 降价超 20%，Grok、Gemini 等 11 条更新”。

本轮实测确认并固化以下规则：

- X榜单是日报选题、排序、文案密度与媒体的主标准；先完整查看榜单事件组，再查看其列出的信源。X 原帖及关联帖只补全同一事件，不能改选题或重排。
- 读者正文直接写新闻事实，不出现“榜单称、主帖称、相关帖、事件组”等采编过程语言；HTML 不出现 X 链接，来源仅存入内部文件。
- 候选图片必须人工判断相关性、清晰度和完整性；不合格就不用。采用的图片只放在对应新闻卡一次，按 `01`、`01-1` 命名；`article.md` 与 `wechat.html` 必须在相同新闻位置引用同一张图。
- 视频条目以视频封面作为日报配图；可从同一 X 原帖稳定获取的原视频保存到当天 `assets/videos/` 并登记。网页不嵌入视频播放器。
- 每张采用素材都要在 `assets/materials.md` 说明对应内容、文章位置和视频使用位置；自动检查素材名单、文件名，以及 Markdown/HTML 的引用与顺序一致。
- 标题在内部生成 3 个候选后定稿，产品名和核心变化靠前，数字必须真实；禁用空泛标题党词及“XX 发布了”式标题。

本次完整日报采用 6 张相关且清晰的图片／视频封面；Sol 与 Runway Ruby 的两条原视频已下载并登记，供后续剪辑使用。页面宽度、正文采编语言与 Markdown 漏图问题均已修正；日报输出校验已通过。

## 10. 分期实施顺序

### 阶段 0：方案沉淀（已完成）

- 固化本文。
- 明确零付费存储与发布策略。
- 完成 X榜单页面可访问性验证。

### 阶段 1：研究管道（第一版完成，持续迭代）

1. 为 X榜单赛道榜和长文榜提取结构化候选条目。
2. 新增 5–8 个互补来源的只读适配器。
3. 建立候选去重、分类和评分规则。
4. 选取 2–3 个样本主题，验证素材清单、下载、资料包输出。
5. 为上述步骤写可重复运行的检查脚本。

完成标志：能在一次运行中得到“多源候选池 + 选题建议 + 一个样本主题的素材与资料包”。（已达成）

### 阶段 2：AI 日报 Skill（已完成完整日报实测，持续迭代）

1. 把成熟研究管道封装为 `ai-daily`。
2. 生成 Markdown、来源记录、公众号 HTML、图片/视频封面、可下载原视频与素材说明。
3. 连续用多个日期试运行，复核标题、正文密度、页面受限提示、素材关联性/清晰度与排版。（后续优化）

完成标志：用户只需调用一次 Skill，就能复制发布一篇日报；页面受限时能安全停在用户决策点，素材、Markdown 与 HTML 通过交付验收。（已完成首篇完整日报并通过输出校验；后续以多日期日报持续校准。）

### 阶段 3：AI 长文 Skill

1. 在同一工程中实现 `ai-longform`。
2. 增加文章角度设计、大纲、图解建议和教程步骤能力。
3. 用长文榜与官方资料完成多个不同类型样本。

完成标志：用户只需给一个方向，即可获得可发布的原创长文包。

### 阶段 4：视频 Skill

1. 在制作前确定两套视频视觉规范。
2. 实现文章到口播稿、TTS、词级字幕和 HyperFrames 渲染。
3. 分别验证知识讲解和实操教程模板。

完成标志：任一文章输入均可输出可上传的视频成品包。

### 阶段 5：网站

1. 定义 GitHub 内容仓库结构和发布索引。
2. 建立日报、长文、视频三个展示入口。
3. 连接域名，验证 Git 提交到网站更新流程。

完成标志：只提交内容文件即可自动更新网站。

## 11. 当前下一步

日报 Skill 已完成首篇完整日报实测。下一步是再用多个日期复核标题、正文、页面受限提示、素材关联性与排版；稳定后开始 AI 长文 Skill。
